//Create a fixed array and use pointers to transverse through elements and print the output.

#include <stdio.h>

int main()
{
    int arr[4]={10,20,30,40};
    int *ptr=arr;
    int i=0;
    for(i=0;i<4;i++)
    {
        printf("%d\t",ptr[i]);
    }
    

    return 0;
}
